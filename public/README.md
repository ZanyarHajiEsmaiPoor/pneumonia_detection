# Place Your Model Here

Place your `pneumonia_model.onnx` file in this directory.

The application expects to find the model at: `public/pneumonia_model.onnx`

Without the model file, the application will show an error when you try to upload an image.
